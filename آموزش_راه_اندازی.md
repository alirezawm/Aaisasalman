# 🚀 آموزش کامل راه‌اندازی اپلیکیشن آسیا سلمان

## 📋 پیش‌نیازها

### سیستم مورد نیاز
- **سیستم عامل**: Ubuntu 20.04+ / CentOS 8+ / Debian 11+
- **حافظه**: حداقل 2GB، توصیه 4GB+
- **فضای ذخیره**: حداقل 10GB فضای خالی
- **پردازنده**: 2 هسته یا بیشتر

### نرم‌افزارهای مورد نیاز
- Docker 20.10+
- Docker Compose 2.0+
- Git
- curl

## 🔧 مرحله 1: نصب Docker و Docker Compose

### برای Ubuntu/Debian:
```bash
# به‌روزرسانی لیست پکیج‌ها
sudo apt update

# نصب Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# اضافه کردن کاربر به گروه docker
sudo usermod -aG docker $USER

# نصب Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# خروج و ورود مجدد برای اعمال تغییرات گروه
```

### برای CentOS/RHEL:
```bash
# نصب Docker
sudo yum install -y yum-utils
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
sudo yum install -y docker-ce docker-ce-cli containerd.io

# راه‌اندازی سرویس Docker
sudo systemctl start docker
sudo systemctl enable docker

# اضافه کردن کاربر به گروه docker
sudo usermod -aG docker $USER

# نصب Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

## 📁 مرحله 2: دانلود و تنظیم اپلیکیشن

```bash
# کلون کردن پروژه
git clone <آدرس-ریپازیتوری-شما>
cd asiasalman

# قابل اجرا کردن اسکریپت‌های deployment
chmod +x scripts/deploy.sh
chmod +x scripts/backup.sh

# کپی کردن فایل تنظیمات محیط
cp env.example .env

# ویرایش متغیرهای محیط
nano .env
```

## ⚙️ مرحله 3: تنظیم متغیرهای محیط

فایل `.env` را ویرایش کنید:

```bash
# تنظیمات ضروری
SECRET_KEY=کلید-مخفی-شما-اینجا-بگذارید
FLASK_ENV=production
DOMAIN_NAME=دامنه-شما.com

# تنظیمات دیتابیس
SQLALCHEMY_DATABASE_URI=sqlite:///asia_salman.db

# تنظیمات Redis
REDIS_URL=redis://redis:6379/0

# تنظیمات ایمیل (اختیاری)
MAIL_SERVER=smtp.gmail.com
MAIL_USERNAME=ایمیل-شما@gmail.com
MAIL_PASSWORD=رمز-عبور-اپلیکیشن-شما

# تنظیمات API تدبیر
TADBIR_API_URL=https://آدرس-API-تدبیر-شما.com
TADBIR_USERNAME=نام-کاربری-شما
TADBIR_PASSWORD=رمز-عبور-شما
```

## 🚀 مرحله 4: راه‌اندازی اپلیکیشن

```bash
# اجرای اسکریپت deployment
./scripts/deploy.sh deploy
```

اسکریپت deployment به صورت خودکار:
- پیش‌نیازها را بررسی می‌کند
- پوشه‌های لازم را ایجاد می‌کند
- Docker images را می‌سازد
- تمام سرویس‌ها را راه‌اندازی می‌کند
- migration های دیتابیس را اجرا می‌کند
- health check انجام می‌دهد

## 🐳 مرحله 5: بررسی سرویس‌ها

### بررسی وضعیت سرویس‌ها:
```bash
# وضعیت سرویس‌ها
./scripts/deploy.sh status

# مشاهده لاگ‌ها
./scripts/deploy.sh logs

# بررسی سلامت سیستم
curl http://localhost/health
```

### سرویس‌های موجود:
- **Web Application**: پورت 8000 (داخلی)
- **Redis Cache**: پورت 6379
- **Nginx Proxy**: پورت 80 (HTTP) و 443 (HTTPS)

## 🌐 مرحله 6: دسترسی به اپلیکیشن

پس از راه‌اندازی موفق، می‌توانید از طریق آدرس‌های زیر به اپلیکیشن دسترسی پیدا کنید:

- **صفحه اصلی**: `http://آدرس-سرور-شما`
- **پنل مدیریت**: `http://آدرس-سرور-شما/admin`
- **بررسی سلامت**: `http://آدرس-سرور-شما/health`

## 🔒 مرحله 7: تنظیم فایروال

```bash
# اجازه دسترسی HTTP و HTTPS
sudo ufw allow 80
sudo ufw allow 443

# اجازه دسترسی SSH (در صورت نیاز)
sudo ufw allow 22

# فعال کردن فایروال
sudo ufw enable
```

## 🔄 مرحله 8: مدیریت و نگهداری

### دستورات مدیریتی:
```bash
# توقف سرویس‌ها
./scripts/deploy.sh stop

# راه‌اندازی سرویس‌ها
./scripts/deploy.sh start

# راه‌اندازی مجدد سرویس‌ها
./scripts/deploy.sh restart

# بازگشت به نسخه قبلی
./scripts/deploy.sh rollback
```

### پشتیبان‌گیری:
```bash
# پشتیبان‌گیری دستی
docker-compose run --rm backup

# برنامه‌ریزی پشتیبان‌گیری روزانه (اضافه کردن به crontab)
0 2 * * * cd /مسیر/به/asiasalman && docker-compose run --rm backup
```

## 🔧 مرحله 9: عیب‌یابی

### مشکلات رایج:

#### 1. پورت در حال استفاده:
```bash
# بررسی استفاده از پورت
sudo netstat -tulpn | grep :80

# کشتن پروسه یا تغییر پورت در docker-compose.yaml
```

#### 2. مشکل دسترسی:
```bash
# تصحیح دسترسی فایل‌ها
sudo chown -R $USER:$USER .
chmod -R 755 .
```

#### 3. مشکل اتصال دیتابیس:
```bash
# بررسی دسترسی فایل دیتابیس
ls -la instance/

# ایجاد مجدد دیتابیس
docker-compose exec web python -c "
from app import app
from models import db
with app.app_context():
    db.create_all()
"
```

#### 4. مشکل حافظه:
```bash
# بررسی استفاده از حافظه
docker stats

# افزایش محدودیت حافظه در docker-compose.yaml
```

## 📊 مرحله 10: مانیتورینگ

### بررسی لاگ‌ها:
```bash
# خطاهای اپلیکیشن
docker-compose logs web | grep ERROR

# خطاهای Nginx
docker-compose logs nginx | grep error

# استفاده از منابع سیستم
docker stats --no-stream
```

### پاسخ Health Check:
```json
{
  "status": "healthy",
  "version": "1.0.0",
  "services": {
    "database": "healthy",
    "redis": "healthy"
  },
  "system": {
    "timestamp": "2024-01-01T12:00:00Z",
    "cpu_percent": 15.2,
    "memory_percent": 45.8,
    "disk_percent": 23.1
  }
}
```

## 🔐 مرحله 11: تنظیم SSL/HTTPS

### استفاده از Let's Encrypt:
```bash
# نصب certbot
sudo apt install certbot

# دریافت گواهی SSL
sudo certbot certonly --standalone -d دامنه-شما.com

# کپی گواهی‌ها به پوشه ssl_certs
sudo cp /etc/letsencrypt/live/دامنه-شما.com/fullchain.pem ssl_certs/cert.pem
sudo cp /etc/letsencrypt/live/دامنه-شما.com/privkey.pem ssl_certs/key.pem

# فعال کردن HTTPS در nginx/conf.d/default.conf
```

## ✅ چک‌لیست نهایی

قبل از راه‌اندازی نهایی، مطمئن شوید:

- [ ] متغیرهای محیط تنظیم شده‌اند
- [ ] گواهی‌های SSL نصب شده‌اند
- [ ] فایروال پیکربندی شده است
- [ ] استراتژی پشتیبان‌گیری پیاده‌سازی شده است
- [ ] مانیتورینگ راه‌اندازی شده است
- [ ] چرخش لاگ پیکربندی شده است
- [ ] health check ها کار می‌کنند
- [ ] تست عملکرد انجام شده است
- [ ] اسکن امنیتی انجام شده است

## 🎯 دستورات سریع

```bash
# راه‌اندازی کامل
./scripts/deploy.sh deploy

# بررسی وضعیت
./scripts/deploy.sh status

# مشاهده لاگ‌ها
./scripts/deploy.sh logs

# راه‌اندازی مجدد
./scripts/deploy.sh restart

# توقف
./scripts/deploy.sh stop

# راه‌اندازی
./scripts/deploy.sh start
```

## 📞 پشتیبانی

در صورت بروز مشکل:

1. **بررسی لاگ‌ها**: `./scripts/deploy.sh logs`
2. **بررسی وضعیت**: `./scripts/deploy.sh status`
3. **بررسی سلامت**: `curl http://localhost/health`
4. **بررسی منابع**: `docker stats`

---

**🎉 اپلیکیشن شما آماده استفاده است!**

برای دسترسی به اپلیکیشن، آدرس سرور خود را در مرورگر وارد کنید.
