# 📋 خلاصه راه‌اندازی پروژه آسیا سلمان در سرور لینوکس

## 🎯 هدف
راه‌اندازی کامل پروژه آسیا سلمان در سرور لینوکس با IP `192.168.1.4` و پورت `8081`

## ⚠️ هشدار مهم
این راه‌اندازی شامل موارد زیر است:
- ✅ پاک کردن تمام سایت‌های قبلی
- ✅ غیرفعال کردن تمام پورت‌ها به جز SSH (2222) و اپلیکیشن (8081)
- ✅ بهینه‌سازی کامل سرور
- ✅ پاک کردن فایل‌های اضافی

## 🚀 روش سریع (توصیه شده)

### مرحله 1: اتصال به سرور
```bash
ssh -p 2222 root@192.168.1.4
```

### مرحله 2: دانلود و اجرای اسکریپت خودکار
```bash
# کپی کردن محتویات فایل setup_server.sh در سرور
nano setup_server.sh
# محتویات فایل را کپی کنید

# قابل اجرا کردن و اجرا
chmod +x setup_server.sh
./setup_server.sh
```

## 📁 فایل‌های ایجاد شده

### 1. اسکریپت راه‌اندازی خودکار
- **فایل**: `setup_server.sh`
- **توضیح**: اسکریپت کامل راه‌اندازی خودکار
- **استفاده**: `./setup_server.sh`

### 2. آموزش کامل
- **فایل**: `آموزش_راه_اندازی_سرور_لینوکس.md`
- **توضیح**: آموزش کامل مرحله به مرحله
- **استفاده**: مرجع کامل برای راه‌اندازی دستی

### 3. راهنمای سریع
- **فایل**: `README_سرور_لینوکس.md`
- **توضیح**: راهنمای سریع و دستورات مدیریت
- **استفاده**: مرجع سریع پس از راه‌اندازی

### 4. اسکریپت کپی فایل‌ها
- **فایل**: `deploy_to_server.sh`
- **توضیح**: کپی فایل‌ها از سیستم محلی به سرور
- **استفاده**: `./deploy_to_server.sh`

## 🔧 مراحل راه‌اندازی خودکار

اسکریپت `setup_server.sh` به صورت خودکار مراحل زیر را انجام می‌دهد:

1. **پاک‌سازی کامل سرور**
   - توقف تمام سرویس‌های وب
   - حذف Apache, Nginx, PHP, Node.js
   - پاک کردن فایل‌های اضافی

2. **تنظیم فایروال**
   - غیرفعال کردن تمام پورت‌ها
   - باز کردن فقط SSH (2222) و اپلیکیشن (8081)

3. **نصب Docker**
   - نصب Docker و Docker Compose
   - تنظیمات بهینه

4. **آماده‌سازی پروژه**
   - کلون کردن از Git
   - ایجاد فایل‌های تنظیمات

5. **راه‌اندازی اپلیکیشن**
   - ساخت Docker images
   - راه‌اندازی سرویس‌ها

6. **بهینه‌سازی سیستم**
   - تنظیمات kernel
   - تنظیمات Docker
   - تنظیمات Nginx

7. **ایجاد ابزارهای مدیریت**
   - اسکریپت‌های مدیریتی
   - سیستم مانیتورینگ
   - پشتیبان‌گیری خودکار

## 📊 دستورات مدیریت پس از راه‌اندازی

```bash
# بررسی وضعیت
/root/manage.sh status

# راه‌اندازی
/root/manage.sh start

# توقف
/root/manage.sh stop

# راه‌اندازی مجدد
/root/manage.sh restart

# مشاهده لاگ‌ها
/root/manage.sh logs

# به‌روزرسانی
/root/manage.sh update

# پشتیبان‌گیری
/root/manage.sh backup

# پاک‌سازی
/root/manage.sh clean

# مانیتورینگ
/root/monitor.sh
```

## 🌐 دسترسی به اپلیکیشن

پس از راه‌اندازی موفق:

- **صفحه اصلی**: http://192.168.1.4:8081
- **پنل مدیریت**: http://192.168.1.4:8081/admin
- **بررسی سلامت**: http://192.168.1.4:8081/health

## ⚙️ تنظیمات مهم

### فایل .env
```bash
SECRET_KEY=asia-salman-super-secret-key-2024-production
FLASK_ENV=production
DOMAIN_NAME=192.168.1.4
PORT=8081
HOST=0.0.0.0
```

### پورت‌های باز
- **2222**: SSH
- **8081**: اپلیکیشن آسیا سلمان
- **80**: Nginx (اختیاری)
- **443**: HTTPS (اختیاری)

## 🔍 عیب‌یابی

### مشکلات رایج:

1. **اپلیکیشن راه‌اندازی نمی‌شود**
   ```bash
   /root/manage.sh logs
   /root/manage.sh restart
   ```

2. **دسترسی به اپلیکیشن ندارید**
   ```bash
   ufw status
   netstat -tulpn | grep :8081
   ```

3. **مشکل حافظه**
   ```bash
   /root/monitor.sh
   docker stats
   ```

## 📞 پشتیبانی

در صورت بروز مشکل:

1. ابتدا `/root/manage.sh status` را اجرا کنید
2. لاگ‌ها را با `/root/manage.sh logs` بررسی کنید
3. منابع سیستم را با `/root/monitor.sh` چک کنید
4. در صورت نیاز `/root/manage.sh restart` را اجرا کنید

## ✅ چک‌لیست نهایی

قبل از راه‌اندازی:
- [ ] دسترسی root به سرور
- [ ] اتصال اینترنت
- [ ] فضای کافی (حداقل 10GB)

پس از راه‌اندازی:
- [ ] اپلیکیشن در http://192.168.1.4:8081 در دسترس است
- [ ] `/root/manage.sh status` وضعیت healthy نشان می‌دهد
- [ ] فایروال فقط پورت‌های 2222 و 8081 را باز کرده است
- [ ] پشتیبان‌گیری خودکار تنظیم شده است

---

**🎉 اپلیکیشن آسیا سلمان آماده استفاده است!**

**آدرس دسترسی**: http://192.168.1.4:8081
