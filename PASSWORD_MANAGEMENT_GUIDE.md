# راهنمای مدیریت رمز عبور کاربران - سیستم امنیتی پیشرفته

## مگاپرامپت حرفه‌ای برای مدیریت رمز عبور کاربران

این سیستم مدیریت رمز عبور یک راه‌حل امن و جامع برای مشاهده، مدیریت و کنترل رمزهای عبور کاربران در پنل مدیریت است.

## ⚠️ نکات امنیتی مهم

### 🔒 رمزهای عبور چگونه ذخیره می‌شوند؟
- **رمزهای اصلی قابل بازیابی نیستند** - آنها به صورت هش شده ذخیره می‌شوند
- **الگوریتم PBKDF2** - از الگوریتم امن PBKDF2 برای هش کردن استفاده می‌شود
- **Salt منحصر به فرد** - هر رمز salt مخصوص خود را دارد
- **هش یک‌طرفه** - امکان تبدیل هش به رمز اصلی وجود ندارد

### 🛡️ چرا نمی‌توان رمز اصلی را دید؟
```
رمز اصلی: "mypassword123"
↓ (PBKDF2 + Salt)
هش نهایی: "pbkdf2:sha256:260000$randomsalt$hashvalue"
```

## ویژگی‌های سیستم مدیریت رمز عبور

### 1. مشاهده اطلاعات رمز عبور (`/admin/users/<user_id>/password`)

#### اطلاعات نمایش داده شده:
- **نوع الگوریتم هش** (مثل pbkdf2:sha256)
- **تاریخ ایجاد حساب کاربری**
- **هش کامل رمز عبور** (برای اهداف دیباگ)
- **جزئیات فنی** (Salt, Hash Value)

#### قابلیت‌های نمایش:
- **نمایش/مخفی کردن هش** با کلیک روی آیکون چشم
- **کپی کردن هش** با یک کلیک
- **تست API** برای دریافت اطلاعات JSON

### 2. تغییر رمز عبور توسط ادمین

#### ویژگی‌ها:
- **تغییر رمز عبور** با رمز جدید دلخواه
- **اعتبارسنجی** (حداقل 6 کاراکتر)
- **تأیید رمز عبور** برای جلوگیری از خطا
- **لاگ‌گیری کامل** عملیات

#### نحوه استفاده:
1. رمز عبور جدید را وارد کنید
2. رمز عبور را تأیید کنید
3. روی "تغییر رمز عبور" کلیک کنید
4. رمز عبور فوراً تغییر می‌یابد

### 3. تولید رمز عبور تصادفی

#### ویژگی‌ها:
- **رمز عبور 12 کاراکتری** امن
- **ترکیب حروف، اعداد و نمادها**
- **نمایش یک‌باره** رمز تولید شده
- **لاگ‌گیری** عملیات تولید

#### نحوه استفاده:
1. روی "تولید رمز عبور" کلیک کنید
2. رمز تولید شده را یادداشت کنید
3. رمز فوراً اعمال می‌شود

## API برای دریافت اطلاعات رمز عبور

### Endpoint: `/api/admin/users/<user_id>/password-hash`

#### درخواست:
```http
GET /api/admin/users/123/password-hash
Authorization: Bearer <admin_token>
```

#### پاسخ:
```json
{
    "user_id": 123,
    "username": "john_doe",
    "full_name": "جان دو",
    "password_hash": "pbkdf2:sha256:260000$randomsalt$hashvalue",
    "hash_method": "pbkdf2:sha256",
    "salt": "randomsalt",
    "hash_value": "hashvalue",
    "created_at": "2024-01-15T10:30:00Z"
}
```

## نحوه دسترسی به سیستم

### 1. از طریق لیست کاربران:
1. به `/admin/users` بروید
2. روی آیکون کلید (🔑) کنار هر کاربر کلیک کنید

### 2. از طریق جزئیات کاربر:
1. به `/admin/users/<user_id>` بروید
2. روی "مدیریت رمز عبور" کلیک کنید

### 3. دسترسی مستقیم:
- URL: `/admin/users/<user_id>/password`

## کنترل دسترسی و امنیت

### مجوزهای مورد نیاز:
- `is_admin = True` یا
- `has_permission('user.manage')`

### لاگ‌گیری امنیتی:
تمام عملیات در جدول `audit_log` ثبت می‌شوند:
- **تغییر رمز عبور**: `reset_user_password`
- **تولید رمز عبور**: `generate_user_password`
- **مشاهده هش**: `view_password_hash`

### اطلاعات لاگ:
```json
{
    "action": "reset_user_password",
    "actor_id": 1,
    "target_id": 123,
    "details": {
        "username": "john_doe",
        "admin_username": "admin"
    },
    "ip_address": "192.168.1.100",
    "user_agent": "Mozilla/5.0...",
    "created_at": "2024-01-15T10:30:00Z"
}
```

## مثال‌های عملی

### مثال 1: تغییر رمز عبور کاربر
```python
# در routes.py
user.password_hash = generate_password_hash("new_password_123")
models.db.session.commit()
```

### مثال 2: تولید رمز عبور تصادفی
```python
import secrets
import string

alphabet = string.ascii_letters + string.digits + "!@#$%^&*"
new_password = ''.join(secrets.choice(alphabet) for i in range(12))
user.password_hash = generate_password_hash(new_password)
```

### مثال 3: بررسی هش رمز عبور
```python
# بررسی صحت رمز عبور
if check_password_hash(user.password_hash, "entered_password"):
    print("رمز عبور صحیح است")
else:
    print("رمز عبور اشتباه است")
```

## نکات مهم برای ادمین‌ها

### ✅ کارهای مجاز:
- مشاهده هش رمز عبور برای اهداف دیباگ
- تغییر رمز عبور کاربران
- تولید رمز عبور امن جدید
- بررسی اطلاعات امنیتی

### ❌ کارهای غیرممکن:
- مشاهده رمز عبور اصلی (چون هش شده است)
- بازیابی رمز عبور اصلی از هش
- دسترسی بدون مجوز

### 🔐 بهترین شیوه‌ها:
1. **همیشه لاگ‌ها را بررسی کنید**
2. **رمزهای تولید شده را امن نگه دارید**
3. **فقط در صورت ضرورت رمزها را تغییر دهید**
4. **از رمزهای قوی استفاده کنید**

## عیب‌یابی و مشکلات رایج

### مشکل: "Access denied"
**علت**: عدم دسترسی ادمین
**راه‌حل**: اطمینان از `is_admin=True` یا مجوز `user.manage`

### مشکل: "Password too short"
**علت**: رمز عبور کمتر از 6 کاراکتر
**راه‌حل**: استفاده از رمز عبور حداقل 6 کاراکتری

### مشکل: "Passwords don't match"
**علت**: عدم تطبیق رمز عبور و تأیید آن
**راه‌حل**: اطمینان از یکسان بودن رمزهای وارد شده

## امنیت و حریم خصوصی

### محافظت از داده‌ها:
- تمام عملیات نیاز به احراز هویت دارند
- لاگ‌گیری کامل تمام فعالیت‌ها
- عدم ذخیره رمزهای اصلی
- استفاده از HTTPS برای انتقال داده‌ها

### رعایت حریم خصوصی:
- فقط ادمین‌های مجاز دسترسی دارند
- تمام عملیات لاگ می‌شوند
- اطلاع‌رسانی به کاربر در صورت تغییر رمز

## نتیجه‌گیری

این سیستم مدیریت رمز عبور یک راه‌حل امن و حرفه‌ای برای مدیریت رمزهای عبور کاربران است. با رعایت اصول امنیتی و بهترین شیوه‌ها، می‌توانید به راحتی رمزهای عبور کاربران را مدیریت کنید.

**یادآوری مهم**: رمزهای اصلی قابل بازیابی نیستند و این یک ویژگی امنیتی است، نه یک محدودیت!
