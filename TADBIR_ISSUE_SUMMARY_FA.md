# خلاصه مشکل تدبیر و راه‌حل

## 🔍 تشخیص مشکل

تست‌های کامل انجام شده و نتایج به شرح زیر است:

### ✅ چیزهایی که کار می‌کنند:
- **سرور تدبیر**: سرور در آدرس `5.202.90.240:8085` در دسترس است ✓
- **اتصال شبکه**: پورت 8085 باز و قابل دسترسی است ✓
- **API تدبیر**: سرویس API در حال کار است ✓
- **نقاط پایانی**: endpoint های `/` و `/odata` پاسخ می‌دهند (کد 200) ✓

### ❌ مشکل اصلی:
- **احراز هویت**: نام کاربری یا رمز عبور فعلی صحیح نیست ✗
- **خطای دریافتی**: `invalid_grant` (نام کاربری/رمز عبور نامعتبر)
- **اطلاعات فعلی**: 
  ```
  نام کاربری: Asia@tadbir.biz
  رمز عبور: Asia@tadbir.biz
  ```
- **نتیجه**: تمام endpoint ها کد 503 (Service Unavailable) برمی‌گردانند چون احراز هویت موفق نیست

## 📋 راه‌حل (مرحله به مرحله)

### مرحله ۱: دریافت اطلاعات صحیح
با مسئول سرور تدبیر تماس بگیرید و موارد زیر را بپرسید:
- نام کاربری صحیح (Username)
- رمز عبور صحیح (Password)
- آیا حساب کاربری فعال است؟
- آیا دسترسی‌های لازم داده شده؟

### مرحله ۲: تنظیم اطلاعات
پس از دریافت اطلاعات صحیح، اسکریپت زیر را اجرا کنید:

```bash
python setup_tadbir_credentials.py
```

این اسکریپت:
- از شما نام کاربری و رمز عبور را می‌خواهد
- اطلاعات را تست می‌کند
- در صورت صحیح بودن، در پایگاه داده و فایل .env ذخیره می‌کند

### مرحله ۳: تست اتصال
پس از تنظیم، اتصال را تست کنید:

```bash
python test_tadbir_simple.py
```

اگر همه چیز درست باشد، باید پیام "SUCCESS" را ببینید.

## 🛠️ ابزارهای کمکی ایجاد شده

1. **test_tadbir_simple.py** - تست ساده اتصال
2. **test_tadbir_auth.py** - تست کامل روش‌های احراز هویت
3. **setup_tadbir_credentials.py** - تنظیم اطلاعات صحیح
4. **check_tadbir_settings.py** - بررسی تنظیمات پایگاه داده
5. **TADBIR_CONNECTION_FIX.md** - راهنمای کامل

## 📊 گزارش‌های ایجاد شده

تمام تست‌ها گزارش‌هایی ایجاد کرده‌اند:
- `tadbir_test_20251011_093030.json`
- `tadbir_auth_test_20251011_093110.json`

این فایل‌ها شامل جزئیات کامل تست‌ها هستند.

## 🔧 تنظیمات دستی (اختیاری)

اگر می‌خواهید دستی تنظیم کنید:

### 1. ایجاد فایل .env
در پوشه اصلی پروژه فایل `.env` ایجاد کنید:

```env
TADBIR_API_URL=http://5.202.90.240:8085
TADBIR_USERNAME=نام_کاربری_صحیح
TADBIR_PASSWORD=رمز_عبور_صحیح
TADBIR_TIMEOUT=300
TADBIR_RETRY_ATTEMPTS=3
```

### 2. نصب python-dotenv

```bash
pip install python-dotenv
```

### 3. به‌روزرسانی tadbir_api_service.py

فایل `tadbir_api_service.py` باید از فایل .env بخواند:

```python
from dotenv import load_dotenv
load_dotenv()  # در ابتدای فایل
```

## ❓ سوالات متداول

### Q1: چرا همه endpoint ها 503 برمی‌گردانند؟
**پاسخ:** چون احراز هویت موفق نیست، سیستم نمی‌تواند به API دسترسی پیدا کند.

### Q2: سرور تدبیر کار می‌کند؟
**پاسخ:** بله! تست‌ها نشان دادند سرور در دسترس و فعال است.

### Q3: مشکل از کجاست؟
**پاسخ:** فقط از نام کاربری یا رمز عبور که صحیح نیست.

### Q4: چطور می‌فهمم اطلاعات صحیح است؟
**پاسخ:** وقتی اسکریپت `setup_tadbir_credentials.py` را اجرا کنید، اطلاعات را تست می‌کند و نتیجه را نشان می‌دهد.

## 📞 تماس با پشتیبانی

اگر پس از انجام موارد بالا مشکل حل نشد:

### 1. بررسی با مدیر تدبیر:
- حساب کاربری فعال است؟
- نام کاربری و رمز عبور صحیح است؟
- دسترسی‌های لازم داده شده؟
- محدودیت IP یا فایروال وجود دارد؟

### 2. ارسال گزارش‌ها:
فایل‌های JSON ایجاد شده را برای بررسی ارسال کنید.

### 3. بررسی شبکه:
```bash
ping 5.202.90.240
telnet 5.202.90.240 8085
```

## ✅ چک‌لیست نهایی

قبل از تماس با پشتیبانی:

- [ ] اطلاعات صحیح (نام کاربری و رمز عبور) را از مدیر تدبیر گرفتم
- [ ] اسکریپت `setup_tadbir_credentials.py` را اجرا کردم
- [ ] اطلاعات در .env یا پایگاه داده ذخیره شد
- [ ] اسکریپت `test_tadbir_simple.py` را اجرا کردم
- [ ] گزارش‌های JSON را بررسی کردم
- [ ] با مدیر تدبیر تایید کردم که حساب فعال است

## 🎯 نتیجه‌گیری

**مشکل:** نام کاربری یا رمز عبور صحیح نیست

**راه‌حل:** 
1. اطلاعات صحیح را از مدیر تدبیر بگیرید
2. اسکریپت `setup_tadbir_credentials.py` را اجرا کنید
3. اتصال را تست کنید

**زمان تخمینی حل مشکل:** 5-10 دقیقه (بعد از دریافت اطلاعات صحیح)

---

**تاریخ گزارش:** 11 اکتبر 2025  
**نسخه:** 1.0  
**وضعیت:** تشخیص کامل - نیاز به اطلاعات صحیح

