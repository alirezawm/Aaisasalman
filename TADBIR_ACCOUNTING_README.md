# سیستم حسابداری یکپارچه تدبیر

سیستم یکپارچه برای اتصال به نرم‌افزار حسابداری تدبیر و مدیریت کالاها، موجودی و قیمت‌ها

## ویژگی‌ها

### 🔄 همگام‌سازی خودکار
- همگام‌سازی خودکار کالاها، موجودی و قیمت‌ها
- همگام‌سازی افزایشی برای بهبود عملکرد
- زمان‌بندی قابل تنظیم (پیش‌فرض: هر 3 ساعت)
- لاگ کامل عملیات همگام‌سازی

### 📊 مدیریت داده‌ها
- کش کالاهای تدبیر
- کش موجودی انبار شماره 10
- کش قیمت‌های مختلف (تکی چکی، عمده چکی، عمده نقدی)
- محاسبه خودکار قیمت نهایی با درصد اضافی

### 🎛️ پنل مدیریت
- داشبورد جامع وضعیت سیستم
- مدیریت همگام‌سازی دستی
- تاریخچه کامل عملیات
- تنظیمات پیشرفته
- تست اتصال به API

### 🔐 امنیت و پایداری
- احراز هویت امن با توکن
- مدیریت خطا و تلاش مجدد
- لاگ‌گیری کامل
- محافظت از داده‌ها

## نصب و راه‌اندازی

### 1. نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

### 2. راه‌اندازی پایگاه داده

```bash
python init_tadbir_config.py
```

### 3. تنظیم متغیرهای محیطی

فایل `.env` را ایجاد کنید و تنظیمات زیر را اضافه کنید:

```env
# Tadbir API Configuration
TADBIR_API_URL=http://5.202.90.240:8085
TADBIR_USERNAME=Asia@tadbir.biz
TADBIR_PASSWORD=Asia@tadbir.biz
TADBIR_TIMEOUT=300
TADBIR_RETRY_ATTEMPTS=3

# Sync Configuration
SYNC_INTERVAL_HOURS=3
BATCH_SIZE=1000
ENABLE_INCREMENTAL_SYNC=True

# Price Configuration
DEFAULT_MARKUP_PERCENTAGE=10
PRICE_ROUNDING=round
CURRENCY_FORMAT=هزار تومان
```

### 4. اجرای سیستم

```bash
python app.py
```

## استفاده

### دسترسی به پنل مدیریت

1. وارد سیستم شوید
2. از منوی کاربری، "حسابداری تدبیر" را انتخاب کنید
3. به داشبورد مدیریت دسترسی پیدا کنید

### همگام‌سازی دستی

1. در داشبورد، روی "همگام‌سازی دستی" کلیک کنید
2. نوع همگام‌سازی را انتخاب کنید:
   - **همگام‌سازی کامل**: همه داده‌ها
   - **کالاها**: فقط کالاها
   - **موجودی**: فقط موجودی
   - **قیمت‌ها**: فقط قیمت‌ها
3. روی "شروع همگام‌سازی" کلیک کنید

### تنظیمات

در بخش "تنظیمات" می‌توانید:

- **تنظیمات API**: آدرس، نام کاربری، رمز عبور
- **تنظیمات همگام‌سازی**: فاصله زمانی، اندازه دسته
- **تنظیمات قیمت**: درصد اضافی، نوع گرد کردن
- **تنظیمات اعلان**: انواع اعلان‌ها

## API Endpoints

### Admin Endpoints

- `GET /admin/accounting/dashboard` - داشبورد حسابداری
- `POST /admin/accounting/sync/manual` - همگام‌سازی دستی
- `GET /admin/accounting/sync/history` - تاریخچه همگام‌سازی
- `GET /admin/accounting/products` - لیست کالاها
- `GET /admin/accounting/inventory` - موجودی انبار
- `GET /admin/accounting/prices` - قیمت‌ها
- `GET/POST /admin/accounting/settings` - تنظیمات

### API Endpoints

- `POST /api/accounting/test-connection` - تست اتصال
- `GET /api/accounting/sync-status` - وضعیت همگام‌سازی

## ساختار پایگاه داده

### جداول اصلی

- **TadbirSyncLog**: لاگ همگام‌سازی
- **TadbirProductCache**: کش کالاها
- **TadbirPriceCache**: کش قیمت‌ها
- **TadbirInventoryCache**: کش موجودی
- **TadbirSyncSettings**: تنظیمات سیستم

## عیب‌یابی

### مشکلات رایج

1. **خطای اتصال به API**
   - آدرس API را بررسی کنید
   - نام کاربری و رمز عبور را بررسی کنید
   - اتصال اینترنت را بررسی کنید

2. **همگام‌سازی ناموفق**
   - لاگ‌ها را در تاریخچه بررسی کنید
   - تنظیمات API را بررسی کنید
   - اندازه دسته را کاهش دهید

3. **داده‌های ناقص**
   - همگام‌سازی کامل انجام دهید
   - کش را پاک کنید و دوباره همگام‌سازی کنید

### لاگ‌ها

لاگ‌های سیستم در جدول `TadbirSyncLog` ذخیره می‌شوند و شامل:
- نوع همگام‌سازی
- وضعیت (شروع، تکمیل، خطا)
- تعداد رکوردهای پردازش شده
- پیام خطا (در صورت وجود)

## پشتیبانی

برای پشتیبانی و گزارش مشکلات:
- ایمیل: support@asiasalman.com
- تلگرام: @asiasalman
- واتساپ: +989123456789

## مجوز

این سیستم تحت مجوز MIT منتشر شده است.

## تغییرات

### نسخه 1.0.0
- راه‌اندازی اولیه سیستم
- همگام‌سازی خودکار
- پنل مدیریت
- API کامل
