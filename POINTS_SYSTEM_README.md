# سیستم امتیازدهی (Points System)

سیستم امتیازدهی برای پاداش‌دهی به کاربران بر اساس خریدهایشان طراحی شده است.

## ویژگی‌های اصلی

### محاسبه امتیازات
- **امتیاز پایه**: هر 100,000 ریال خرید = 500 امتیاز (قابل تنظیم)
- **امتیاز اضافی**: هر محصول اضافی = 50 امتیاز (قابل تنظیم)
- **حداکثر امتیاز اضافی**: 1000 امتیاز در هر خرید (قابل تنظیم)

### سطح‌بندی کاربران
- **برنزی**: 0+ امتیاز (بدون تخفیف)
- **نقره‌ای**: 1000+ امتیاز (5% تخفیف)
- **طلایی**: 5000+ امتیاز (10% تخفیف)
- **پلاتینیوم**: 10000+ امتیاز (15% تخفیف)

### جوایز
- تخفیف درصدی
- تخفیف مبلغی
- ارسال رایگان
- محصول رایگان

## فایل‌های سیستم

### مدل‌های دیتابیس (`models.py`)
- `UserPoints`: امتیازات کاربران
- `PointsTransaction`: تراکنش‌های امتیازی
- `Reward`: جوایز قابل استفاده
- `RewardRedemption`: استفاده از جوایز
- `UserLevel`: سطح‌بندی کاربران
- `PointsRule`: قوانین امتیازدهی

### سرویس‌ها (`points_service.py`)
- `PointsService`: سرویس اصلی محاسبه و مدیریت امتیازات
- `PointsAnalytics`: تحلیل و آمار سیستم

### مسیرها (`routes.py`)
- `/points`: صفحه امتیازات کاربر
- `/api/points/*`: API های امتیازات
- `/admin/points/*`: پنل مدیریت امتیازات

### قالب‌ها
- `templates/points/user_points.html`: صفحه امتیازات کاربر
- `templates/admin/points/dashboard.html`: داشبورد مدیریت
- `templates/admin/points/users.html`: مدیریت کاربران
- `templates/admin/points/rewards.html`: مدیریت جوایز
- `templates/admin/points/rules.html`: مدیریت قوانین

### فایل‌های استایل و اسکریپت
- `static/css/points.css`: استایل‌های سیستم امتیازدهی
- `static/js/points.js`: اسکریپت‌های تعاملی

## راه‌اندازی

### 1. اجرای اسکریپت راه‌اندازی
```bash
python init_points_system.py
```

### 2. دسترسی‌ها
- **کاربران**: `/points` - مشاهده امتیازات و جوایز
- **مدیریت**: `/admin/points` - مدیریت کامل سیستم

## نحوه استفاده

### برای کاربران
1. ورود به حساب کاربری
2. مشاهده امتیازات در منوی کاربر
3. استفاده از جوایز قابل دسترس
4. مشاهده تاریخچه تراکنش‌ها

### برای مدیران
1. ورود به پنل مدیریت
2. مدیریت امتیازات کاربران
3. ایجاد و مدیریت جوایز
4. تنظیم قوانین امتیازدهی
5. مشاهده آمار و تحلیل‌ها

## API های موجود

### امتیازات کاربر
- `GET /api/points/user` - دریافت امتیازات کاربر
- `GET /api/points/transactions` - تاریخچه تراکنش‌ها
- `GET /api/points/level` - سطح کاربر

### جوایز
- `GET /api/rewards` - جوایز قابل استفاده
- `POST /api/rewards/redeem` - استفاده از جایزه
- `GET /api/rewards/history` - تاریخچه استفاده از جوایز

## تنظیمات پیش‌فرض

### قوانین امتیازدهی
- امتیاز به ازای هر 100 هزار ریال: 500
- امتیاز اضافی به ازای هر محصول: 50
- حداکثر امتیاز اضافی: 1000

### سطح‌بندی
- برنزی: 0-999 امتیاز
- نقره‌ای: 1000-4999 امتیاز
- طلایی: 5000-9999 امتیاز
- پلاتینیوم: 10000+ امتیاز

### جوایز پیش‌فرض
- تخفیف 5 درصدی: 1000 امتیاز
- ارسال رایگان: 500 امتیاز

## نکات مهم

1. **انقضای امتیازات**: امتیازات پس از 12 ماه منقضی می‌شوند
2. **محاسبه خودکار**: امتیازات هنگام ایجاد فاکتور محاسبه می‌شوند
3. **قانون فعال**: فقط یک قانون امتیازدهی می‌تواند فعال باشد
4. **امنیت**: تنظیم امتیازات فقط برای مدیران امکان‌پذیر است

## عیب‌یابی

### مشکلات رایج
1. **عدم نمایش امتیازات**: بررسی ایجاد جداول دیتابیس
2. **خطا در محاسبه**: بررسی قانون فعال امتیازدهی
3. **مشکل در استفاده از جوایز**: بررسی امتیاز کافی کاربر

### لاگ‌ها
- خطاهای سیستم در کنسول Flask نمایش داده می‌شوند
- تراکنش‌های امتیازی در دیتابیس ثبت می‌شوند

## توسعه

### اضافه کردن نوع جایزه جدید
1. افزودن نوع جدید به `reward_type` در مدل `Reward`
2. پیاده‌سازی منطق استفاده در `PointsService.redeem_reward`
3. به‌روزرسانی رابط کاربری

### تغییر قوانین امتیازدهی
1. ایجاد قانون جدید در پنل مدیریت
2. فعال کردن قانون جدید
3. غیرفعال کردن قانون قبلی

## پشتیبانی

برای پشتیبانی و گزارش مشکلات، با تیم توسعه تماس بگیرید.


