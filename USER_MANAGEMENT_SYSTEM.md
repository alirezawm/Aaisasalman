# سیستم مدیریت کاربران - راهنمای کامل

## مگاپرامپت حرفه‌ای برای سیستم مدیریت کاربران

این سیستم مدیریت کاربران یک راه‌حل جامع و حرفه‌ای برای مدیریت، نظارت و کنترل کاربران در پنل مدیریت است. این سیستم با استفاده از Flask، SQLAlchemy و Bootstrap طراحی شده و از RTL و زبان فارسی پشتیبانی کامل می‌کند.

## ویژگی‌های کلیدی سیستم

### 1. داشبورد مدیریت کاربران (`/admin/users`)
- **نمایش لیست کامل کاربران** با قابلیت صفحه‌بندی
- **جستجوی پیشرفته** بر اساس نام کاربری، نام کامل، ایمیل، تلفن و نام شرکت
- **فیلترهای هوشمند** بر اساس نوع کاربر (مدیر، خریدار عمده، عادی) و وضعیت (فعال/غیرفعال)
- **مرتب‌سازی چندگانه** بر اساس تاریخ ثبت، نام کاربری، نام کامل، ایمیل و آخرین ورود
- **آمار لحظه‌ای** شامل تعداد کل کاربران، کاربران فعال، مدیران و خریداران عمده
- **رابط کاربری مدرن** با طراحی ریسپانسیو و پشتیبانی کامل از RTL

### 2. جزئیات کامل کاربر (`/admin/users/<user_id>`)
- **اطلاعات شخصی کامل** شامل تمام فیلدهای پروفایل کاربر
- **اطلاعات مالی** شامل امتیازات، پروفایل فاکتور و اعتبارات
- **فعالیت‌های کاربر** شامل آمار خرید، جستجوها و فعالیت‌های اخیر
- **تاریخچه فاکتورها** با امکان مشاهده جزئیات
- **نقش‌ها و مجوزها** تخصیص داده شده به کاربر
- **تب‌های سازمان‌یافته** برای دسترسی آسان به اطلاعات مختلف

### 3. عملیات مدیریت کاربران
- **حذف امن کاربران** با تأیید دو مرحله‌ای و لاگ کامل
- **تغییر وضعیت کاربران** (فعال/غیرفعال) با امکان بازگشت
- **محافظت از خود** (مدیران نمی‌توانند خودشان را حذف یا غیرفعال کنند)
- **لاگ‌گیری کامل** تمام عملیات برای اهداف حسابرسی

### 4. سیستم امنیتی و مجوزها
- **کنترل دسترسی** بر اساس نقش‌ها و مجوزهای کاربر
- **احراز هویت** برای تمام عملیات حساس
- **لاگ‌گیری امنیتی** تمام عملیات با جزئیات کامل
- **محافظت از داده‌ها** با استفاده از CSRF و اعتبارسنجی

## ساختار فایل‌ها

### 1. Routes (مسیرها)
```python
# در فایل routes.py
@app.route('/admin/users')                    # لیست کاربران
@app.route('/admin/users/<int:user_id>')     # جزئیات کاربر
@app.route('/admin/users/<int:user_id>/delete', methods=['POST'])  # حذف کاربر
@app.route('/admin/users/<int:user_id>/toggle-status', methods=['POST'])  # تغییر وضعیت
@app.route('/api/admin/users/statistics')    # آمار کاربران
```

### 2. Templates (قالب‌ها)
```
templates/admin/
├── users.html          # صفحه لیست کاربران
└── user_detail.html    # صفحه جزئیات کاربر
```

### 3. Models (مدل‌ها)
سیستم از مدل‌های موجود در `models.py` استفاده می‌کند:
- `User` - اطلاعات اصلی کاربر
- `UserPoints` - امتیازات کاربر
- `CustomerInvoiceProfile` - پروفایل فاکتور مشتری
- `UserRole` - نقش‌های کاربر
- `AuditLog` - لاگ‌های حسابرسی

## نحوه استفاده

### 1. دسترسی به سیستم
1. وارد پنل مدیریت شوید (`/admin`)
2. از منوی کناری روی "مدیریت کاربران" کلیک کنید
3. یا مستقیماً به `/admin/users` بروید

### 2. جستجو و فیلتر
- **جستجو**: در کادر جستجو عبارت مورد نظر را وارد کنید
- **فیلتر نوع کاربر**: از منوی کشویی نوع کاربر را انتخاب کنید
- **فیلتر وضعیت**: کاربران فعال یا غیرفعال را فیلتر کنید
- **مرتب‌سازی**: ستون مورد نظر و ترتیب صعودی/نزولی را انتخاب کنید

### 3. مشاهده جزئیات کاربر
- روی آیکون چشم (👁️) در کنار هر کاربر کلیک کنید
- یا روی نام کاربر کلیک کنید تا به صفحه جزئیات بروید

### 4. عملیات مدیریت
- **تغییر وضعیت**: روی آیکون کاربر (✅/❌) کلیک کنید
- **حذف کاربر**: روی آیکون سطل زباله (🗑️) کلیک کنید
- **تأیید عملیات**: در پنجره تأیید که باز می‌شود، عملیات را تأیید کنید

## ویژگی‌های پیشرفته

### 1. صفحه‌بندی هوشمند
- نمایش 20 کاربر در هر صفحه (قابل تنظیم)
- ناوبری آسان بین صفحات
- حفظ فیلترها و جستجو در هنگام تغییر صفحه

### 2. آمار لحظه‌ای
- تعداد کل کاربران
- کاربران فعال و غیرفعال
- تعداد مدیران و خریداران عمده
- آمار ثبت‌نام در 30 روز گذشته

### 3. رابط کاربری مدرن
- طراحی ریسپانسیو برای تمام دستگاه‌ها
- پشتیبانی کامل از RTL و زبان فارسی
- آیکون‌های FontAwesome برای تجربه بصری بهتر
- انیمیشن‌ها و انتقال‌های نرم

### 4. امنیت و محافظت
- تأیید دو مرحله‌ای برای عملیات حساس
- لاگ‌گیری کامل تمام عملیات
- محافظت از حذف خود
- اعتبارسنجی ورودی‌ها

## API Endpoints

### 1. آمار کاربران
```
GET /api/admin/users/statistics
```
**پاسخ:**
```json
{
    "total_users": 150,
    "active_users": 142,
    "inactive_users": 8,
    "admin_users": 3,
    "bulk_buyer_users": 25,
    "regular_users": 122,
    "recent_registrations": 12,
    "user_types": {...},
    "monthly_registrations": [...]
}
```

## تنظیمات و سفارشی‌سازی

### 1. تغییر تعداد کاربران در هر صفحه
در فایل `routes.py`، خط 548:
```python
per_page = request.args.get('per_page', 20, type=int)  # تغییر 20 به عدد مورد نظر
```

### 2. اضافه کردن فیلترهای جدید
در تابع `admin_users()` فیلتر جدید اضافه کنید:
```python
# مثال: فیلتر بر اساس تاریخ ثبت
date_filter = request.args.get('date_from', '')
if date_filter:
    query = query.filter(User.created_at >= datetime.strptime(date_filter, '%Y-%m-%d'))
```

### 3. اضافه کردن ستون‌های جدید
در فایل `users.html` ستون جدید اضافه کنید:
```html
<th>ستون جدید</th>
<!-- در tbody -->
<td>{{ user.field_name }}</td>
```

## عیب‌یابی و نگهداری

### 1. بررسی لاگ‌ها
- تمام عملیات در جدول `audit_log` ثبت می‌شوند
- برای بررسی لاگ‌ها به `/admin/audit-logs` بروید

### 2. پشتیبان‌گیری
- قبل از حذف کاربران مهم، از داده‌ها پشتیبان تهیه کنید
- سیستم به صورت خودکار لاگ کامل عملیات را نگه می‌دارد

### 3. بهینه‌سازی عملکرد
- برای پایگاه‌های داده بزرگ، ایندکس‌های مناسب اضافه کنید
- از کش برای آمار پرتکرار استفاده کنید

## امنیت و بهترین شیوه‌ها

### 1. کنترل دسترسی
- فقط کاربران با مجوز `user.manage` یا `is_admin=True` دسترسی دارند
- تمام عملیات حساس نیاز به تأیید دارند

### 2. اعتبارسنجی
- تمام ورودی‌ها اعتبارسنجی می‌شوند
- از CSRF token برای محافظت از فرم‌ها استفاده می‌شود

### 3. لاگ‌گیری
- تمام عملیات حساس لاگ می‌شوند
- اطلاعات IP و User-Agent ذخیره می‌شوند

## نتیجه‌گیری

این سیستم مدیریت کاربران یک راه‌حل جامع، امن و کاربرپسند برای مدیریت کاربران در پنل مدیریت است. با ویژگی‌های پیشرفته، رابط کاربری مدرن و امنیت بالا، تمام نیازهای مدیریت کاربران را پوشش می‌دهد.

برای استفاده از این سیستم، کافی است به `/admin/users` بروید و از امکانات آن استفاده کنید. سیستم به صورت خودکار تمام عملیات را مدیریت و لاگ می‌کند.
