# سیستم سبد خرید پیشرفته - راهنمای پیاده‌سازی

## خلاصه پیاده‌سازی

سیستم سبد خرید پیشرفته با موفقیت پیاده‌سازی شد و شامل قابلیت‌های مدرن و تجربه کاربری برتر است.

## فایل‌های ایجاد شده

### 1. مدل‌های دیتابیس (models.py)
- **Cart**: مدل پیشرفته سبد خرید با قابلیت‌های جدید
- **CartSession**: مدیریت جلسات سبد خرید
- **Wishlist**: لیست علاقه‌مندی‌ها
- **CartNotification**: سیستم اعلان‌های هوشمند

### 2. API های جدید (routes.py)
- `/api/cart` - دریافت محتویات سبد خرید
- `/api/cart/add` - اضافه کردن محصول به سبد
- `/api/cart/update` - به‌روزرسانی آیتم سبد
- `/api/cart/remove` - حذف آیتم از سبد
- `/api/cart/clear` - خالی کردن سبد
- `/api/cart/save-for-later` - ذخیره برای بعد
- `/api/cart/saved` - دریافت آیتم‌های ذخیره شده
- `/api/cart/totals` - محاسبه مجموع قیمت‌ها
- `/api/cart/count` - شمارش آیتم‌های سبد
- `/api/cart/summary` - خلاصه سبد با پیشنهادات
- `/api/wishlist/*` - مدیریت لیست علاقه‌مندی‌ها
- `/api/notifications/*` - مدیریت اعلان‌ها
- `/api/analytics/*` - سیستم تحلیل و ردیابی

### 3. رابط کاربری
- **cart-system.js**: سیستم اصلی سبد خرید
- **cart-notifications.js**: سیستم اعلان‌ها
- **cart-analytics.js**: سیستم تحلیل و ردیابی
- **cart-system.css**: استایل‌های مدرن و واکنش‌گرا

### 4. قالب‌های HTML
- **base.html**: به‌روزرسانی شده با سبد خرید جدید
- **shop.html**: اضافه شدن دکمه‌های علاقه‌مندی

### 5. مهاجرت دیتابیس
- **database_migration.sql**: اسکریپت مهاجرت کامل

## ویژگی‌های کلیدی

### 🛒 سبد خرید پیشرفته
- اضافه/حذف محصولات با انیمیشن
- تغییر تعداد با دکمه‌های + و -
- ذخیره برای بعد
- محاسبه لحظه‌ای قیمت‌ها
- پشتیبانی از تخفیف‌های پویا
- انقضای خودکار آیتم‌ها

### 💝 لیست علاقه‌مندی‌ها
- اضافه کردن محصولات به علاقه‌مندی‌ها
- انتقال از علاقه‌مندی‌ها به سبد خرید
- مدیریت و حذف آیتم‌ها

### 🔔 سیستم اعلان‌ها
- اعلان‌های هوشمند برای رویدادهای سبد خرید
- کاهش قیمت محصولات
- هشدار موجودی
- یادآوری سبد خرید رها شده
- اعلان‌های زمان‌بندی شده

### 📊 تحلیل و ردیابی
- ردیابی رفتار کاربران
- تحلیل رها کردن سبد خرید
- ردیابی تبدیل سبد به سفارش
- آمار محصولات محبوب
- تحلیل جستجوها

### 🎨 تجربه کاربری
- انیمیشن‌های روان
- طراحی واکنش‌گرا
- پشتیبانی از موبایل
- دسترسی‌پذیری
- حالت تاریک
- میانبرهای صفحه‌کلید

### 🔧 قابلیت‌های فنی
- کش کردن داده‌ها
- بارگذاری تدریجی
- بهینه‌سازی عملکرد
- امنیت پیشرفته
- پشتیبانی از حالت آفلاین

## نحوه استفاده

### 1. اجرای مهاجرت دیتابیس
```bash
sqlite3 instance/asia_salman.db < database_migration.sql
```

### 2. راه‌اندازی سرور
```bash
python app.py
```

### 3. تست قابلیت‌ها
- ورود به سیستم
- اضافه کردن محصولات به سبد خرید
- تست لیست علاقه‌مندی‌ها
- بررسی اعلان‌ها
- تست تحلیل و ردیابی

## API Documentation

### Cart Management
```javascript
// اضافه کردن محصول
POST /api/cart/add
{
    "product_id": 1,
    "quantity": 2,
    "price_type": "cash",
    "notes": "یادداشت"
}

// به‌روزرسانی آیتم
PUT /api/cart/update
{
    "cart_id": 1,
    "quantity": 3,
    "notes": "یادداشت جدید"
}

// حذف آیتم
DELETE /api/cart/remove
{
    "cart_id": 1
}
```

### Wishlist Management
```javascript
// اضافه کردن به علاقه‌مندی‌ها
POST /api/wishlist/add
{
    "product_id": 1,
    "notes": "یادداشت"
}

// انتقال به سبد خرید
POST /api/wishlist/move-to-cart
{
    "wishlist_id": 1,
    "quantity": 2,
    "price_type": "cash"
}
```

### Notifications
```javascript
// دریافت اعلان‌ها
GET /api/notifications

// علامت‌گذاری به عنوان خوانده شده
PUT /api/notifications/read
{
    "notification_id": 1
}
```

## تنظیمات پیشرفته

### 1. تنظیم انقضای سبد خرید
```python
# در models.py
expires_at = db.Column(db.DateTime, default=lambda: datetime.utcnow() + timedelta(days=30))
```

### 2. تنظیم فاصله اعلان‌ها
```javascript
// در cart-notifications.js
this.pollingInterval = 30000; // 30 ثانیه
```

### 3. تنظیم کش
```python
# در routes.py
# می‌توانید Redis یا Memcached اضافه کنید
```

## بهینه‌سازی عملکرد

### 1. ایندکس‌های دیتابیس
- ایندکس روی user_id, product_id, price_type
- ایندکس روی session_id
- ایندکس روی expires_at

### 2. کش کردن
- کش محتویات سبد خرید
- کش قیمت‌ها
- کش پیشنهادات

### 3. بارگذاری تدریجی
- بارگذاری تدریجی آیتم‌های سبد
- بارگذاری تدریجی تصاویر
- بارگذاری تدریجی پیشنهادات

## امنیت

### 1. اعتبارسنجی
- اعتبارسنجی ورودی‌ها
- بررسی دسترسی کاربر
- محافظت CSRF

### 2. رمزگذاری
- رمزگذاری داده‌های حساس
- محافظت XSS
- محافظت SQL Injection

## پشتیبانی از موبایل

### 1. طراحی واکنش‌گرا
- سایدبار سبد خرید
- دکمه شناور
- اعلان‌های موبایل

### 2. عملکرد
- بهینه‌سازی برای شبکه‌های موبایل
- پشتیبانی از حالت آفلاین
- تجربه شبیه اپلیکیشن

## دسترسی‌پذیری

### 1. صفحه‌خوان
- پشتیبانی از خواننده صفحه
- ناوبری با صفحه‌کلید
- برچسب‌های مناسب

### 2. کنتراست
- حالت کنتراست بالا
- تغییر اندازه فونت
- کاهش حرکت

## تست و دیباگ

### 1. تست عملکرد
```javascript
// بررسی عملکرد سبد خرید
console.log(window.cartSystem.exportAnalytics());
```

### 2. تست اعلان‌ها
```javascript
// تست اعلان جدید
window.cartNotifications.showToast('تست اعلان', 'success');
```

### 3. تست تحلیل
```javascript
// بررسی آمار
console.log(window.cartAnalytics.getTopProducts());
```

## عیب‌یابی

### 1. مشکلات رایج
- سبد خرید خالی می‌شود: بررسی انقضای آیتم‌ها
- اعلان‌ها نمایش داده نمی‌شوند: بررسی API
- عملکرد کند: بررسی ایندکس‌های دیتابیس

### 2. لاگ‌ها
- بررسی لاگ‌های سرور
- بررسی کنسول مرورگر
- بررسی شبکه

## به‌روزرسانی‌های آینده

### 1. قابلیت‌های پیشنهادی
- پیشنهادات مبتنی بر هوش مصنوعی
- خرید با صدا
- واقعیت افزوده
- بلاک‌چین

### 2. بهینه‌سازی‌ها
- بهبود عملکرد
- کاهش مصرف حافظه
- بهینه‌سازی شبکه

## پشتیبانی

برای پشتیبانی و گزارش مشکلات:
- بررسی مستندات
- بررسی کد منبع
- تماس با تیم فنی

---

**نکته**: این سیستم با آخرین استانداردهای وب توسعه یافته و برای استفاده در محیط‌های تولید آماده است.
