# راهنمای اصلاح قیمت‌های تدبیر
## Tadbir Price Conversion Fix Guide

## مشکل
قیمت‌های دریافتی از تدبیر به **ریال** هستند، اما قبلاً بدون تبدیل در دیتابیس ذخیره می‌شدند. 
سیستم انتظار دارد قیمت‌ها به **هزار ریال** باشند.

**مثال:**
- قیمت تدبیر: `1,999,250,000` ریال
- قیمت صحیح در دیتابیس: `1,999,250` هزار ریال (÷ 1000)

## راه‌حل اعمال شده

### 1. تغییرات کد
فایل `tadbir_sync_service.py` اصلاح شد:
- قیمت پایه (base_price): `÷ 1000`
- مبلغ تخفیف (discount_amount): `÷ 1000`

### 2. همگام‌سازی مجدد

برای اعمال تغییرات، باید قیمت‌ها را مجدداً از تدبیر دریافت کنید:

#### روش 1: استفاده از اسکریپت (توصیه می‌شود)
```bash
python fix_tadbir_prices.py
```

این اسکریپت:
1. ✓ قیمت‌های قدیمی را پاک می‌کند
2. ✓ قیمت‌های جدید را با تبدیل صحیح دریافت می‌کند
3. ✓ قیمت‌ها را به محصولات اعمال می‌کند

#### روش 2: از طریق پنل ادمین
1. وارد پنل ادمین شوید
2. بخش **حسابداری تدبیر** → **همگام‌سازی**
3. کلیک روی **"Clear Price Cache"** (اگر موجود است)
4. کلیک روی **"Sync Prices"**
5. کلیک روی **"Sync Prices to Products"**

## نتیجه

پس از اجرا:
- ✓ قیمت پایه: `1,999,250` هزار ریال
- ✓ قیمت نهایی: `2,199,175` هزار ریال
- ✓ نمایش: `1,999,250,000` ریال (× 1000 برای نمایش)

## نمایش قیمت‌ها

قیمت‌ها در دیتابیس به **هزار ریال** ذخیره می‌شوند و برای نمایش در 1000 ضرب می‌شوند:

### در Template ها:
```jinja2
{{ "{:,.0f}".format(product.bulk_price_cash * 1000) }} ریال
```

### در JavaScript:
```javascript
var fullPrice = price * 1000;  // Convert to Rials for display
```

## بررسی صحت

بعد از همگام‌سازی، قیمت‌ها را بررسی کنید:
1. در پنل ادمین → **قیمت‌های حسابداری**
2. قیمت پایه باید عددی حدود `1,000,000` تا `5,000,000` (هزار ریال) باشد
3. اگر عدد خیلی بزرگ است (مثل `1,999,250,000`) یعنی تبدیل نشده است

---

## Technical Details

### Files Modified:
- `tadbir_sync_service.py` (lines 351, 369, 384)

### Conversion Formula:
```python
# قیمت تدبیر به ریال است، تبدیل به هزار ریال
base_price = float(price_data.get('Price', 0)) / 1000
discount_amount = float(price_data.get('Discount', 0)) / 1000
```

### Storage Convention:
- Database: Thousands of Rials (هزار ریال)
- Display: Rials (ریال) = DB_Value × 1000
- Tadbir API: Rials (ریال) → Convert to DB by ÷ 1000

---

تاریخ: 2025-10-11
نسخه: 1.0

