# سیستم مدیریت فاکتورهای مشتریان

## 📋 معرفی

سیستم مدیریت فاکتورهای مشتریان یک سیستم کامل برای مدیریت، تایید، رد و اطلاع‌رسانی فاکتورهای مشتریان در داشبورد مدیر سیستم است.

## ✨ ویژگی‌ها

### 🎯 ویژگی‌های اصلی
- **نمایش تمام فاکتورهای مشتریان** با فیلترهای پیشرفته
- **مشاهده جزئیات کامل فاکتور** و مستندات
- **تایید فاکتور** با امکان اضافه کردن یادداشت
- **رد فاکتور** با ذکر دلیل و اطلاع‌رسانی به مشتری
- **سیستم اطلاع‌رسانی خودکار** به مشتریان
- **مدیریت مستندات فاکتور** (چک، رسید و غیره)
- **گزارش‌گیری و آمار** فاکتورها

### 🔧 ویژگی‌های فنی
- **فیلترهای پیشرفته**: وضعیت، نوع پرداخت، مشتری، تاریخ، مبلغ
- **جستجوی زنده**: جستجوی فوری در فاکتورها
- **صفحه‌بندی**: نمایش بهینه نتایج
- **مدال‌های تعاملی**: تایید، رد و بررسی فاکتورها
- **نمایشگر تصاویر**: مشاهده مستندات فاکتور
- **API های RESTful**: برای جستجو و آمار

## 🚀 نصب و راه‌اندازی

### 1. اجرای Migration دیتابیس

```bash
# اجرای فایل migration
sqlite3 instance/asia_salman.db < invoice_management_migration.sql
```

### 2. راه‌اندازی سیستم

```bash
# اجرای برنامه
python app.py
```

### 3. دسترسی به سیستم

- **URL**: `/admin/invoices/management`
- **دسترسی**: ادمین یا order_manager

## 📱 راهنمای استفاده

### 🏠 صفحه اصلی مدیریت فاکتورها

1. **آمار کلی**: نمایش 6 کارت آمار شامل:
   - کل فاکتورها
   - در انتظار تایید
   - تایید شده
   - رد شده
   - در حال بررسی
   - مبلغ کل (میلیون ریال)

2. **فیلترهای پیشرفته**:
   - وضعیت تایید
   - نوع پرداخت
   - جستجوی مشتری
   - محدوده تاریخ
   - محدوده مبلغ

3. **جدول فاکتورها**:
   - نمایش اطلاعات کامل فاکتور
   - عملیات تایید، رد، بررسی
   - لینک مشاهده جزئیات

### 🔍 عملیات روی فاکتورها

#### تایید فاکتور
1. کلیک روی دکمه "تایید"
2. وارد کردن یادداشت (اختیاری)
3. انتخاب ارسال اطلاع‌رسانی
4. تایید عملیات

#### رد فاکتور
1. کلیک روی دکمه "رد"
2. وارد کردن دلیل رد (اجباری)
3. وارد کردن یادداشت (اختیاری)
4. انتخاب ارسال اطلاع‌رسانی
5. تایید عملیات

#### بررسی فاکتور
1. کلیک روی دکمه "بررسی"
2. وارد کردن یادداشت (اختیاری)
3. تایید عملیات

### 📄 مدیریت مستندات

1. **مشاهده مستندات**: کلیک روی "مشاهده" در جدول فاکتورها
2. **تایید مستند**: کلیک روی دکمه "تایید" در کنار مستند
3. **رد مستند**: کلیک روی دکمه "رد" و وارد کردن دلیل

## 🔧 API ها

### آمار فاکتورها
```http
GET /api/admin/invoices/statistics
```

**پاسخ**:
```json
{
  "total_invoices": 150,
  "pending_approval": 25,
  "approved": 100,
  "rejected": 15,
  "under_review": 10,
  "total_amount": 2500.5,
  "cash_invoices": 80,
  "check_invoices": 70
}
```

### جستجوی فاکتورها
```http
GET /api/admin/invoices/search?approval_status=pending&payment_type=cash
```

**پارامترها**:
- `approval_status`: وضعیت تایید
- `payment_type`: نوع پرداخت
- `user_search`: جستجوی مشتری
- `date_from`: تاریخ از
- `date_to`: تاریخ تا
- `amount_min`: حداقل مبلغ
- `amount_max`: حداکثر مبلغ
- `page`: شماره صفحه
- `per_page`: تعداد در هر صفحه

## 🔔 سیستم اطلاع‌رسانی

### انواع اطلاع‌رسانی

1. **تایید فاکتور**:
   - عنوان: "✅ فاکتور شما تایید شد"
   - محتوا: شماره فاکتور، مبلغ، یادداشت ادمین

2. **رد فاکتور**:
   - عنوان: "❌ فاکتور شما رد شد"
   - محتوا: شماره فاکتور، دلیل رد، یادداشت ادمین

3. **بررسی فاکتور**:
   - عنوان: "🔍 فاکتور شما در حال بررسی است"
   - محتوا: شماره فاکتور، یادداشت ادمین

### تنظیمات اطلاع‌رسانی

- **ارسال خودکار**: هنگام تایید/رد فاکتور
- **قالب پیام**: قابل تنظیم در `invoice_notification_service.py`
- **ذخیره در دیتابیس**: تمام اطلاع‌رسانی‌ها ذخیره می‌شوند

## 🗄️ ساختار دیتابیس

### تغییرات جدول `invoice`
```sql
ALTER TABLE invoice ADD COLUMN admin_review_notes TEXT;
ALTER TABLE invoice ADD COLUMN notification_sent BOOLEAN DEFAULT FALSE;
ALTER TABLE invoice ADD COLUMN notification_sent_at DATETIME;
```

### تغییرات جدول `user_notification`
```sql
ALTER TABLE user_notification ADD COLUMN related_invoice_id INTEGER;
ALTER TABLE user_notification ADD COLUMN notification_action VARCHAR(50);
```

## 🧪 تست

### اجرای تست‌ها
```bash
python test_invoice_management.py
```

### تست‌های موجود
- ایجاد فاکتور
- ارسال اطلاع‌رسانی تایید
- ارسال اطلاع‌رسانی رد
- ارسال اطلاع‌رسانی بررسی
- API آمار فاکتورها
- API جستجوی فاکتورها
- روت‌های تایید، رد و بررسی

## 🔐 امنیت

### کنترل دسترسی
- **ادمین**: دسترسی کامل
- **order_manager**: دسترسی کامل
- **کاربران عادی**: بدون دسترسی

### اعتبارسنجی
- اعتبارسنجی ورودی‌های فرم
- بررسی وجود فاکتور
- بررسی دسترسی کاربر

## 🎨 سفارشی‌سازی

### استایل‌ها
- فایل CSS: `static/css/invoice-management.css`
- تم‌های رنگی قابل تنظیم
- طراحی ریسپانسیو

### اسکریپت‌ها
- فایل JS: `static/js/invoice-management.js`
- قابلیت‌های تعاملی
- جستجوی زنده

## 🐛 عیب‌یابی

### مشکلات رایج

1. **خطای دسترسی**:
   - بررسی نقش کاربر
   - بررسی لاگین بودن

2. **خطای دیتابیس**:
   - اجرای migration
   - بررسی اتصال دیتابیس

3. **خطای اطلاع‌رسانی**:
   - بررسی سرویس اطلاع‌رسانی
   - بررسی لاگ‌ها

### لاگ‌ها
```python
import logging
logging.basicConfig(level=logging.INFO)
```

## 📈 بهینه‌سازی

### عملکرد
- ایندکس‌گذاری جداول
- کش کردن آمار
- صفحه‌بندی نتایج

### امنیت
- اعتبارسنجی ورودی‌ها
- محدودیت دسترسی
- لاگ‌گیری عملیات

## 🔄 بروزرسانی

### نسخه‌های آینده
- سیستم امضای دیجیتال
- اتصال به سیستم حسابداری
- گزارش‌گیری پیشرفته
- اتوماسیون تایید فاکتورها

## 📞 پشتیبانی

برای گزارش مشکلات یا درخواست ویژگی‌های جدید، لطفاً با تیم توسعه تماس بگیرید.

---

**نسخه**: 1.0  
**تاریخ**: 2025-01-27  
**توسعه‌دهنده**: تیم توسعه سیستم
