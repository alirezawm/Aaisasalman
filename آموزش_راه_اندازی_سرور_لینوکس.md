# 🚀 آموزش کامل راه‌اندازی پروژه آسیا سلمان در سرور لینوکس

## 📋 مشخصات سرور
- **آدرس IP**: 192.168.1.4
- **پورت**: 8081
- **مسیر نصب**: `/root/application/`
- **ریپازیتوری Git**: https://git.agarvand.ir/Alirezawm/asiasalman.git
- **SSH پورت**: 2222

## ⚠️ هشدار مهم - پاک‌سازی کامل سرور

**این آموزش تمام سایت‌های موجود در سرور را پاک می‌کند و تمام پورت‌ها به جز SSH (2222) را غیرفعال می‌کند.**

## 🔧 مرحله 1: اتصال به سرور و پاک‌سازی کامل

### 1.1 اتصال به سرور
```bash
ssh -p 2222 root@192.168.1.4
```

### 1.2 توقف تمام سرویس‌های وب
```bash
# توقف Apache
systemctl stop apache2 2>/dev/null || systemctl stop httpd 2>/dev/null || true

# توقف Nginx
systemctl stop nginx 2>/dev/null || true

# توقف تمام سرویس‌های Docker
docker stop $(docker ps -aq) 2>/dev/null || true
docker rm $(docker ps -aq) 2>/dev/null || true

# توقف تمام سرویس‌های Python/Flask
pkill -f python 2>/dev/null || true
pkill -f gunicorn 2>/dev/null || true
pkill -f uwsgi 2>/dev/null || true

# توقف تمام سرویس‌های Node.js
pkill -f node 2>/dev/null || true
pkill -f npm 2>/dev/null || true
pkill -f yarn 2>/dev/null || true

# توقف تمام سرویس‌های PHP
systemctl stop php*-fpm 2>/dev/null || true
```

### 1.3 پاک‌سازی کامل وب سرورها
```bash
# پاک کردن Apache
apt-get remove --purge apache2 apache2-utils apache2-bin apache2-data -y 2>/dev/null || true
yum remove httpd -y 2>/dev/null || true

# پاک کردن Nginx
apt-get remove --purge nginx nginx-common nginx-core -y 2>/dev/null || true
yum remove nginx -y 2>/dev/null || true

# پاک کردن PHP
apt-get remove --purge php* -y 2>/dev/null || true
yum remove php* -y 2>/dev/null || true

# پاک کردن Node.js
apt-get remove --purge nodejs npm -y 2>/dev/null || true
yum remove nodejs npm -y 2>/dev/null || true

# پاک کردن Python اضافی
apt-get remove --purge python3-pip python3-venv -y 2>/dev/null || true
yum remove python3-pip -y 2>/dev/null || true
```

### 1.4 پاک‌سازی فایل‌های اضافی
```bash
# پاک کردن تمام فایل‌های وب
rm -rf /var/www/* 2>/dev/null || true
rm -rf /usr/share/nginx/* 2>/dev/null || true
rm -rf /home/*/public_html 2>/dev/null || true

# پاک کردن لاگ‌های قدیمی
rm -rf /var/log/apache2/* 2>/dev/null || true
rm -rf /var/log/nginx/* 2>/dev/null || true
rm -rf /var/log/php* 2>/dev/null || true

# پاک کردن فایل‌های موقت
rm -rf /tmp/*
rm -rf /var/tmp/*

# پاک کردن cache
rm -rf /var/cache/apt/archives/* 2>/dev/null || true
rm -rf /var/cache/yum/* 2>/dev/null || true
```

## 🔒 مرحله 2: تنظیم فایروال و پورت‌ها

### 2.1 غیرفعال کردن تمام پورت‌ها به جز SSH
```bash
# نصب و تنظیم UFW
apt-get update
apt-get install ufw -y

# تنظیم قوانین فایروال
ufw --force reset
ufw default deny incoming
ufw default allow outgoing

# اجازه SSH فقط روی پورت 2222
ufw allow 2222/tcp

# اجازه پورت 8081 برای اپلیکیشن جدید
ufw allow 8081/tcp

# فعال کردن فایروال
ufw --force enable

# نمایش وضعیت فایروال
ufw status verbose
```

### 2.2 غیرفعال کردن سرویس‌های غیرضروری
```bash
# غیرفعال کردن سرویس‌های غیرضروری
systemctl disable apache2 2>/dev/null || true
systemctl disable httpd 2>/dev/null || true
systemctl disable nginx 2>/dev/null || true
systemctl disable mysql 2>/dev/null || true
systemctl disable postgresql 2>/dev/null || true
systemctl disable php*-fpm 2>/dev/null || true

# توقف سرویس‌های غیرضروری
systemctl stop apache2 2>/dev/null || true
systemctl stop httpd 2>/dev/null || true
systemctl stop nginx 2>/dev/null || true
systemctl stop mysql 2>/dev/null || true
systemctl stop postgresql 2>/dev/null || true
systemctl stop php*-fpm 2>/dev/null || true
```

## 🐳 مرحله 3: نصب Docker و Docker Compose

### 3.1 نصب Docker
```bash
# حذف نسخه‌های قدیمی Docker
apt-get remove docker docker-engine docker.io containerd runc -y 2>/dev/null || true

# نصب پیش‌نیازها
apt-get update
apt-get install -y \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# اضافه کردن کلید GPG رسمی Docker
mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# اضافه کردن repository Docker
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null

# نصب Docker Engine
apt-get update
apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# راه‌اندازی سرویس Docker
systemctl start docker
systemctl enable docker

# اضافه کردن کاربر root به گروه docker
usermod -aG docker root
```

### 3.2 نصب Docker Compose
```bash
# دانلود آخرین نسخه Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

# قابل اجرا کردن
chmod +x /usr/local/bin/docker-compose

# ایجاد لینک سمبلیک
ln -s /usr/local/bin/docker-compose /usr/bin/docker-compose

# بررسی نصب
docker-compose --version
```

## 📁 مرحله 4: آماده‌سازی محیط پروژه

### 4.1 ایجاد مسیر پروژه
```bash
# ایجاد مسیر اصلی
mkdir -p /root/application
cd /root/application

# حذف محتویات قبلی (اگر وجود دارد)
rm -rf * .*

# کلون کردن پروژه
git clone https://git.agarvand.ir/Alirezawm/asiasalman.git .

# تنظیم دسترسی‌ها
chown -R root:root /root/application
chmod -R 755 /root/application
```

### 4.2 ایجاد فایل‌های تنظیمات

#### ایجاد فایل .env
```bash
cat > .env << 'EOF'
# تنظیمات اصلی
SECRET_KEY=your-super-secret-key-change-this-in-production
FLASK_ENV=production
FLASK_APP=app.py
DOMAIN_NAME=192.168.1.4

# تنظیمات دیتابیس
SQLALCHEMY_DATABASE_URI=sqlite:///asia_salman.db
SQLALCHEMY_TRACK_MODIFICATIONS=False

# تنظیمات آپلود
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=16777216

# تنظیمات Redis
REDIS_URL=redis://redis:6379/0

# تنظیمات ایمیل (اختیاری)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# تنظیمات API تدبیر
TADBIR_API_URL=https://your-tadbir-api.com
TADBIR_USERNAME=your-username
TADBIR_PASSWORD=your-password

# تنظیمات ISACO
ENABLE_ISACO_WH15=True
ISACO_BRAND_ID=63
ISACO_WAREHOUSE_ID=15
ISACO_ALLOWED_PLANS=isaco_cash,isaco_1m,isaco_2m,isaco_3m

# تنظیمات پورت
PORT=8081
HOST=0.0.0.0
EOF
```

#### ایجاد فایل docker-compose.yaml
```bash
cat > docker-compose.yaml << 'EOF'
version: '3.8'

services:
  web:
    build: .
    container_name: asiasalman_web
    restart: unless-stopped
    ports:
      - "8081:8000"
    environment:
      - FLASK_ENV=production
      - FLASK_APP=app.py
    volumes:
      - ./instance:/usr/src/app/instance
      - ./uploads:/usr/src/app/uploads
      - ./logs:/usr/src/app/logs
      - ./backups:/usr/src/app/backups
    depends_on:
      - redis
    networks:
      - asiasalman_network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  redis:
    image: redis:7-alpine
    container_name: asiasalman_redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - asiasalman_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  nginx:
    image: nginx:alpine
    container_name: asiasalman_nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./ssl_certs:/etc/nginx/ssl:ro
    depends_on:
      - web
    networks:
      - asiasalman_network

volumes:
  redis_data:

networks:
  asiasalman_network:
    driver: bridge
EOF
```

#### ایجاد پیکربندی Nginx
```bash
# ایجاد پوشه‌های Nginx
mkdir -p nginx/conf.d ssl_certs

# ایجاد فایل nginx.conf
cat > nginx/nginx.conf << 'EOF'
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Logging
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    # Performance
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 20M;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml;

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
    limit_req_zone $binary_remote_addr zone=login:10m rate=5r/m;

    # Include server configurations
    include /etc/nginx/conf.d/*.conf;
}
EOF

# ایجاد فایل تنظیمات سایت
cat > nginx/conf.d/default.conf << 'EOF'
upstream asiasalman_backend {
    server web:8000;
    keepalive 32;
}

server {
    listen 80;
    server_name 192.168.1.4;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Rate limiting
    limit_req zone=api burst=20 nodelay;
    limit_req zone=login burst=5 nodelay;

    # Static files
    location /static/ {
        alias /usr/src/app/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location /uploads/ {
        alias /usr/src/app/uploads/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Main application
    location / {
        proxy_pass http://asiasalman_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
        
        # Buffer settings
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
    }

    # Health check
    location /health {
        proxy_pass http://asiasalman_backend/health;
        access_log off;
    }
}
EOF
```

## 🚀 مرحله 5: راه‌اندازی اپلیکیشن

### 5.1 ساخت و راه‌اندازی
```bash
# بازگشت به مسیر پروژه
cd /root/application

# ساخت Docker images
docker-compose build --no-cache

# راه‌اندازی سرویس‌ها
docker-compose up -d

# بررسی وضعیت سرویس‌ها
docker-compose ps

# مشاهده لاگ‌ها
docker-compose logs -f
```

### 5.2 بررسی سلامت سیستم
```bash
# بررسی سلامت اپلیکیشن
curl -f http://192.168.1.4:8081/health

# بررسی وضعیت Docker
docker stats --no-stream

# بررسی استفاده از پورت‌ها
netstat -tulpn | grep :8081
netstat -tulpn | grep :80
```

## 🔧 مرحله 6: بهینه‌سازی سرور

### 6.1 بهینه‌سازی سیستم
```bash
# تنظیمات kernel برای عملکرد بهتر
cat >> /etc/sysctl.conf << 'EOF'
# Network optimizations
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
net.ipv4.tcp_rmem = 4096 65536 16777216
net.ipv4.tcp_wmem = 4096 65536 16777216
net.core.netdev_max_backlog = 5000
net.ipv4.tcp_congestion_control = bbr

# File system optimizations
fs.file-max = 2097152
vm.swappiness = 10
vm.dirty_ratio = 15
vm.dirty_background_ratio = 5
EOF

# اعمال تنظیمات
sysctl -p

# بهینه‌سازی حافظه
echo 'vm.swappiness=10' >> /etc/sysctl.conf
echo 'vm.dirty_ratio=15' >> /etc/sysctl.conf
echo 'vm.dirty_background_ratio=5' >> /etc/sysctl.conf
```

### 6.2 تنظیمات Docker
```bash
# تنظیمات Docker daemon
cat > /etc/docker/daemon.json << 'EOF'
{
    "log-driver": "json-file",
    "log-opts": {
        "max-size": "10m",
        "max-file": "3"
    },
    "storage-driver": "overlay2",
    "live-restore": true,
    "userland-proxy": false,
    "experimental": false,
    "metrics-addr": "0.0.0.0:9323",
    "default-ulimits": {
        "memlock": {
            "Hard": -1,
            "Name": "memlock",
            "Soft": -1
        }
    }
}
EOF

# راه‌اندازی مجدد Docker
systemctl restart docker
```

### 6.3 تنظیمات cron برای نگهداری
```bash
# ایجاد اسکریپت نگهداری
cat > /root/maintenance.sh << 'EOF'
#!/bin/bash

# پاک کردن لاگ‌های قدیمی
find /root/application/logs -name "*.log" -mtime +7 -delete

# پاک کردن Docker images غیرضروری
docker system prune -f

# پشتیبان‌گیری از دیتابیس
cd /root/application
docker-compose exec -T web python -c "
from app import app
from models import db
import sqlite3
import shutil
from datetime import datetime

with app.app_context():
    timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
    backup_path = f'/usr/src/app/backups/asia_salman_{timestamp}.db'
    
    # کپی دیتابیس
    shutil.copy2('/usr/src/app/instance/asia_salman.db', backup_path)
    print(f'Database backed up to {backup_path}')
"

# حذف پشتیبان‌های قدیمی (بیش از 30 روز)
find /root/application/backups -name "*.db" -mtime +30 -delete

echo "Maintenance completed at $(date)"
EOF

# قابل اجرا کردن اسکریپت
chmod +x /root/maintenance.sh

# اضافه کردن به cron (هر روز ساعت 2 صبح)
echo "0 2 * * * /root/maintenance.sh >> /var/log/maintenance.log 2>&1" | crontab -
```

## 🔄 مرحله 7: اسکریپت‌های مدیریتی

### 7.1 اسکریپت مدیریت کامل
```bash
cat > /root/manage.sh << 'EOF'
#!/bin/bash

APP_DIR="/root/application"
COMPOSE_FILE="$APP_DIR/docker-compose.yaml"

case "$1" in
    start)
        echo "Starting Asia Salman application..."
        cd $APP_DIR
        docker-compose up -d
        echo "Application started successfully!"
        ;;
    stop)
        echo "Stopping Asia Salman application..."
        cd $APP_DIR
        docker-compose down
        echo "Application stopped successfully!"
        ;;
    restart)
        echo "Restarting Asia Salman application..."
        cd $APP_DIR
        docker-compose restart
        echo "Application restarted successfully!"
        ;;
    status)
        echo "Checking application status..."
        cd $APP_DIR
        docker-compose ps
        echo ""
        echo "Health check:"
        curl -f http://192.168.1.4:8081/health 2>/dev/null && echo "✅ Application is healthy" || echo "❌ Application is not responding"
        ;;
    logs)
        echo "Showing application logs..."
        cd $APP_DIR
        docker-compose logs -f
        ;;
    update)
        echo "Updating application..."
        cd $APP_DIR
        git pull
        docker-compose build --no-cache
        docker-compose up -d
        echo "Application updated successfully!"
        ;;
    backup)
        echo "Creating backup..."
        cd $APP_DIR
        ./maintenance.sh
        echo "Backup completed!"
        ;;
    clean)
        echo "Cleaning up..."
        cd $APP_DIR
        docker system prune -f
        docker volume prune -f
        echo "Cleanup completed!"
        ;;
    *)
        echo "Usage: $0 {start|stop|restart|status|logs|update|backup|clean}"
        echo ""
        echo "Commands:"
        echo "  start    - Start the application"
        echo "  stop     - Stop the application"
        echo "  restart  - Restart the application"
        echo "  status   - Check application status"
        echo "  logs     - Show application logs"
        echo "  update   - Update application from Git"
        echo "  backup   - Create database backup"
        echo "  clean    - Clean up Docker resources"
        exit 1
        ;;
esac
EOF

chmod +x /root/manage.sh
```

## ✅ مرحله 8: تست نهایی و بررسی

### 8.1 تست عملکرد
```bash
# بررسی وضعیت کلی
/root/manage.sh status

# تست دسترسی به اپلیکیشن
curl -I http://192.168.1.4:8081

# بررسی استفاده از منابع
docker stats --no-stream

# بررسی لاگ‌ها
/root/manage.sh logs
```

### 8.2 بررسی امنیت
```bash
# بررسی پورت‌های باز
netstat -tulpn | grep LISTEN

# بررسی فایروال
ufw status verbose

# بررسی سرویس‌های فعال
systemctl list-units --state=active | grep -E "(apache|nginx|mysql|postgresql)"
```

## 📊 مرحله 9: مانیتورینگ و نگهداری

### 9.1 نصب ابزارهای مانیتورینگ
```bash
# نصب htop برای مانیتورینگ
apt-get install htop iotop nethogs -y

# ایجاد اسکریپت مانیتورینگ
cat > /root/monitor.sh << 'EOF'
#!/bin/bash

echo "=== Asia Salman Server Status ==="
echo "Date: $(date)"
echo ""

echo "=== System Resources ==="
echo "CPU Usage:"
top -bn1 | grep "Cpu(s)" | awk '{print $2}' | awk -F'%' '{print $1}'
echo ""

echo "Memory Usage:"
free -h
echo ""

echo "Disk Usage:"
df -h
echo ""

echo "=== Docker Status ==="
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
echo ""

echo "=== Application Health ==="
curl -s http://192.168.1.4:8081/health | python3 -m json.tool 2>/dev/null || echo "Application not responding"
echo ""

echo "=== Network Connections ==="
netstat -tulpn | grep -E ":(80|443|8081|2222)"
echo ""

echo "=== Firewall Status ==="
ufw status
EOF

chmod +x /root/monitor.sh
```

### 9.2 تنظیم alerting
```bash
# ایجاد اسکریپت alerting
cat > /root/alert.sh << 'EOF'
#!/bin/bash

# بررسی سلامت اپلیکیشن
if ! curl -f http://192.168.1.4:8081/health >/dev/null 2>&1; then
    echo "ALERT: Application is not responding!" | logger -t asiasalman
fi

# بررسی استفاده از حافظه
MEMORY_USAGE=$(free | grep Mem | awk '{printf "%.0f", $3/$2 * 100.0}')
if [ $MEMORY_USAGE -gt 90 ]; then
    echo "ALERT: High memory usage: ${MEMORY_USAGE}%" | logger -t asiasalman
fi

# بررسی استفاده از دیسک
DISK_USAGE=$(df / | tail -1 | awk '{print $5}' | sed 's/%//')
if [ $DISK_USAGE -gt 90 ]; then
    echo "ALERT: High disk usage: ${DISK_USAGE}%" | logger -t asiasalman
fi
EOF

chmod +x /root/alert.sh

# اضافه کردن به cron (هر 5 دقیقه)
echo "*/5 * * * * /root/alert.sh" | crontab -
```

## 🎯 دستورات سریع مدیریت

```bash
# راه‌اندازی اپلیکیشن
/root/manage.sh start

# توقف اپلیکیشن
/root/manage.sh stop

# راه‌اندازی مجدد
/root/manage.sh restart

# بررسی وضعیت
/root/manage.sh status

# مشاهده لاگ‌ها
/root/manage.sh logs

# به‌روزرسانی
/root/manage.sh update

# پشتیبان‌گیری
/root/manage.sh backup

# پاک‌سازی
/root/manage.sh clean

# مانیتورینگ
/root/monitor.sh
```

## 🌐 دسترسی به اپلیکیشن

پس از راه‌اندازی موفق، می‌توانید از طریق آدرس‌های زیر به اپلیکیشن دسترسی پیدا کنید:

- **صفحه اصلی**: http://192.168.1.4:8081
- **پنل مدیریت**: http://192.168.1.4:8081/admin
- **بررسی سلامت**: http://192.168.1.4:8081/health

## 🔐 نکات امنیتی

1. **تغییر SECRET_KEY**: حتماً SECRET_KEY را در فایل `.env` تغییر دهید
2. **تنظیم رمز عبور**: رمزهای عبور API تدبیر را تنظیم کنید
3. **فایروال**: فقط پورت‌های 2222 (SSH) و 8081 (اپلیکیشن) باز هستند
4. **پشتیبان‌گیری**: پشتیبان‌گیری خودکار روزانه تنظیم شده است
5. **مانیتورینگ**: سیستم alerting برای مشکلات مهم تنظیم شده است

## 📞 عیب‌یابی

در صورت بروز مشکل:

1. **بررسی وضعیت**: `/root/manage.sh status`
2. **مشاهده لاگ‌ها**: `/root/manage.sh logs`
3. **بررسی منابع**: `/root/monitor.sh`
4. **راه‌اندازی مجدد**: `/root/manage.sh restart`

---

**🎉 اپلیکیشن آسیا سلمان با موفقیت در سرور لینوکس راه‌اندازی شد!**

**آدرس دسترسی**: http://192.168.1.4:8081
