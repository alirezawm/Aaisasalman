# انتقال دیتابیس به مسیر خارج از پروژه

## خلاصه تغییرات

دیتابیس سایت از مسیر پروژه به یک پوشه خارجی (`../data/`) منتقل شده است. این کار باعث می‌شود که هنگام به‌روزرسانی فایل‌های پروژه، دیتابیس تحت تأثیر قرار نگیرد.

## تغییرات انجام شده

### 1. مسیر جدید دیتابیس
- **مسیر قدیمی**: `instance/asia_salman.db` یا `asia_salman.db` (داخل پروژه)
- **مسیر جدید در سرور**: `/root/data/asia_salman.db`
- **مسیر جدید در محیط محلی**: `../data/asia_salman.db`
- سیستم به صورت خودکار مسیر مناسب را تشخیص می‌دهد

### 2. فایل‌های تغییر یافته

#### `app.py`
- مسیر دیتابیس به صورت خودکار تشخیص داده می‌شود:
  - در سرور (مسیر شروع با `/root/`): `/root/data/asia_salman.db`
  - در محیط محلی: `../data/asia_salman.db`
- می‌توانید با تنظیم متغیر محیطی `DATABASE_DIR` مسیر را تغییر دهید

#### `database_path.py` (جدید)
- ماژول کمکی برای تعیین مسیر دیتابیس در تمام اسکریپت‌ها
- توابع:
  - `get_database_path()`: مسیر جدید دیتابیس را برمی‌گرداند
  - `find_database()`: دیتابیس را در مسیرهای قدیمی یا جدید پیدا می‌کند

#### `restart_app.py` و `fix_database_lock.py`
- به‌روزرسانی شده‌اند تا از مسیر جدید دیتابیس استفاده کنند

#### `.gitignore`
- پوشه `../data/` به لیست فایل‌های نادیده گرفته شده اضافه شده است

### 3. اسکریپت انتقال

`migrate_database_location.py` برای انتقال خودکار دیتابیس از مسیر قدیمی به جدید ایجاد شده است.

## استفاده

### اجرای برنامه
برنامه به صورت خودکار از مسیر جدید دیتابیس استفاده می‌کند. نیازی به تغییر دستی نیست.

### تغییر مسیر دیتابیس (اختیاری)
اگر می‌خواهید مسیر دیگری استفاده کنید، متغیر محیطی `DATABASE_DIR` را تنظیم کنید:

```bash
# Linux/Mac
export DATABASE_DIR=/path/to/your/data/directory

# Windows PowerShell
$env:DATABASE_DIR = "C:\path\to\your\data\directory"

# Windows CMD
set DATABASE_DIR=C:\path\to\your\data\directory
```

### انتقال دستی دیتابیس

#### در محیط محلی (Windows/Linux/Mac):
```bash
python migrate_database_location.py
```

#### در سرور لینوکس:
```bash
# انتقال به /root/data/
python migrate_database_server.py

# یا با دسترسی root
sudo python migrate_database_server.py
```

## مزایا

1. **امنیت داده‌ها**: دیتابیس هنگام به‌روزرسانی پروژه تحت تأثیر قرار نمی‌گیرد
2. **پشتیبان‌گیری آسان**: پوشه `data` را می‌توانید به راحتی پشتیبان‌گیری کنید
3. **مدیریت بهتر**: تمام داده‌های سایت در یک مکان جداگانه قرار دارند
4. **سازگاری**: اسکریپت‌های قدیمی همچنان کار می‌کنند (با جستجو در مسیرهای قدیمی)

## نکات مهم

- پوشه `../data/` به صورت خودکار ایجاد می‌شود
- فایل‌های WAL و SHM نیز به مسیر جدید منتقل می‌شوند
- دیتابیس قدیمی در `instance/` باقی می‌ماند (می‌توانید بعداً حذف کنید)
- برای پشتیبان‌گیری، کافی است پوشه `../data/` را کپی کنید

## ساختار پوشه‌ها

### در محیط محلی (Windows/Linux/Mac):
```
F:\site\final 2\
├── asiasalman/          # پروژه اصلی
│   ├── app.py
│   ├── routes.py
│   └── ...
└── data/                # دیتابیس (خارج از پروژه)
    ├── asia_salman.db
    ├── asia_salman.db-wal
    └── asia_salman.db-shm
```

### در سرور لینوکس:
```
/root/
├── application/         # پروژه اصلی
│   ├── app.py
│   ├── routes.py
│   └── ...
└── data/                # دیتابیس (خارج از پروژه)
    ├── asia_salman.db
    ├── asia_salman.db-wal
    └── asia_salman.db-shm
```

## عیب‌یابی

### اگر برنامه دیتابیس را پیدا نمی‌کند:
1. بررسی کنید که فایل `../data/asia_salman.db` وجود دارد
2. اگر وجود ندارد، از `migrate_database_location.py` استفاده کنید
3. بررسی کنید که دسترسی‌های فایل درست است

### اگر می‌خواهید به مسیر قدیمی برگردید:
در `app.py` خط 27 را تغییر دهید:
```python
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///instance/asia_salman.db'
```

